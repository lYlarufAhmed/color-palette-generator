

function Grid(props){
    // console.log(props)
    return (
        <div className={'grid'} style={{backgroundColor:props.color, opacity:props.opacity}}>
        </div>
    )
}

export default Grid